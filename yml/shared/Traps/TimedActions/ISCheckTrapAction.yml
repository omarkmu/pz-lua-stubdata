version: "1.1"
languages:
  lua:
    classes:
      ISCheckTrapAction:
        extends: ISBaseTimedAction
        constructors:
          - parameters:
              - name: character
                nullable: true
              - name: trap
        staticFields:
          Type:
            defaultValue: '"ISCheckTrapAction"'
        fields:
          trap: {}
        methods:
          - name: isValid
            return:
              - type: boolean
          - name: update
          - name: start
          - name: stop
          - name: perform
          - name: complete
            return:
              - type: boolean
          - name: getDuration
            return:
              - type: number
