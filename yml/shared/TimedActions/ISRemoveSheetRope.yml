version: "1.1"
languages:
  lua:
    classes:
      ISRemoveSheetRope:
        extends: ISBaseTimedAction
        constructors:
          - parameters:
              - name: character
                type: IsoPlayer
              - name: window
                type: IsoObject
        staticFields:
          Type:
            defaultValue: '"ISRemoveSheetRope"'
        fields:
          window:
            type: IsoObject
        methods:
          - name: isValid
            return:
              - type: boolean
          - name: waitToStart
            return:
              - type: boolean
          - name: update
          - name: start
          - name: stop
          - name: perform
          - name: complete
            return:
              - type: boolean
          - name: getDuration
            return:
              - type: number
