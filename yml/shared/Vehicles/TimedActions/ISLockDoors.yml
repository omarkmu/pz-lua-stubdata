version: "1.1"
languages:
  lua:
    classes:
      ISLockDoors:
        extends: ISBaseTimedAction
        constructors:
          - parameters:
              - name: character
              - name: vehicle
              - name: locked
        staticFields:
          Type:
            defaultValue: '"ISLockDoors"'
        fields:
          vehicle: {}
          locked: {}
        methods:
          - name: isValid
            return:
              - type: boolean
          - name: update
          - name: start
          - name: stop
          - name: perform
          - name: complete
            return:
              - type: boolean
                nullable: true
          - name: getDuration
            return:
              - type: number
