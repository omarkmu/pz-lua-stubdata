version: "1.1"
languages:
  lua:
    classes:
      Rabbit:
        extends: ISBaseMover
        constructors:
          - {}
        staticFields:
          Type:
            defaultValue: '"Rabbit"'
        methods:
          - name: init
          - name: update
          - name: postrender
            parameters:
              - name: col
              - name: bDoAttached
    functions:
      - name: testCreateRabbit
