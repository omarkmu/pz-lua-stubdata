version: "1.1"
languages:
  lua:
    classes:
      ItemBindingHandler:
        staticMethods:
          - name: onKeyPressed
            parameters:
              - name: key
                type: integer
          - name: toggleLight
            parameters:
              - name: key
                type: integer
