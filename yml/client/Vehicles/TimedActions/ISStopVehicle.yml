version: "1.1"
languages:
  lua:
    classes:
      ISStopVehicle:
        extends: ISBaseTimedAction
        constructors:
          - parameters:
              - name: character
        staticFields:
          Type:
            defaultValue: '"ISStopVehicle"'
        methods:
          - name: isValid
            return:
              - type: boolean
          - name: update
          - name: start
          - name: stop
          - name: perform
