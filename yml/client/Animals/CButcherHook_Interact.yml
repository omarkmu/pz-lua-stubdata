version: "1.1"
languages:
  lua:
    classes:
      CButcherHook_Interact:
        staticMethods:
          - name: onKeyPress
            parameters:
              - name: key
                type: integer
