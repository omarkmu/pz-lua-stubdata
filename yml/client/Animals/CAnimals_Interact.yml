version: "1.1"
languages:
  lua:
    classes:
      CAnimals_Interact:
        staticMethods:
          - name: onKeyPress
            parameters:
              - name: key
                type: integer
