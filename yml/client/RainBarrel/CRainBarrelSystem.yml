version: "1.1"
languages:
  lua:
    classes:
      CRainBarrelSystem:
        extends: CGlobalObjectSystem
        constructors:
          - {}
        staticFields:
          Type:
            defaultValue: '"CRainBarrelSystem"'
        methods:
          - name: isValidIsoObject
            parameters:
              - name: isoObject
            return:
              - type: boolean
          - name: newLuaObject
            parameters:
              - name: globalObject
            return:
              - type: CRainBarrelGlobalObject
