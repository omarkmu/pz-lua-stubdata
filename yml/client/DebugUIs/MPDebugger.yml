version: "1.1"
languages:
  lua:
    classes:
      MPDebugger:
        extends: ISCollapsableWindow
        staticFields:
          Type:
            defaultValue: '"MPDebugger"'
        staticMethods:
          - name: onKeyPressed
            parameters:
              - name: key
                type: integer
