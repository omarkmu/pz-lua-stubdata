version: "1.1"
languages:
  lua:
    classes:
      ISJoypadDisconnectedUI:
        extends: ISUIElement
        constructors:
          - parameters:
              - name: playerNum
                type: integer
        staticFields:
          Type:
            defaultValue: '"ISJoypadDisconnectedUI"'
        fields:
          label:
            type: ISLabel
          buttonKBM:
            type: ISButton
        methods:
          - name: createChildren
          - name: prerender
          - name: update
          - name: useKeyboardMouse
          - name: renderControllerDisconnected
