version: "1.1"
languages:
  lua:
    classes:
      UI.TextButton:
        extends: UI.Panel
        local: true
        staticFields:
          sounds: {}
        fields:
          super:
            type: UI.Panel
        overloads:
          - parameters:
              - name: args
                type: table
            return:
              - type: UI.TextButton
        methods:
          - name: onHover
            parameters:
              - name: state
          - name: onLeftClick
      PZAPI:
        staticFields:
          UI.TextButton:
            type: UI.TextButton
