version: "1.1"
languages:
  lua:
    classes:
      ISFPS:
        staticFields:
          lastSec:
            defaultValue: "-1"
          frame:
            defaultValue: "0"
          start:
            defaultValue: "false"
          init:
            defaultValue: "false"
        staticMethods:
          - name: onKeyPressed
            parameters:
              - name: key
                type: integer
