version: "1.1"
languages:
  lua:
    classes:
      ISClimbSheetRopeAction:
        extends: ISBaseTimedAction
        constructors:
          - parameters:
              - name: character
                type: IsoPlayer
              - name: down
                type: boolean
        staticFields:
          Type:
            defaultValue: '"ISClimbSheetRopeAction"'
        fields:
          down:
            type: boolean
        methods:
          - name: isValid
            return:
              - type: boolean
          - name: update
          - name: start
          - name: stop
          - name: perform
