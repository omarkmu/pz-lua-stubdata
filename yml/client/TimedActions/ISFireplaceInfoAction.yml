version: "1.1"
languages:
  lua:
    classes:
      ISFireplaceInfoAction:
        extends: ISBaseTimedAction
        constructors:
          - parameters:
              - name: character
              - name: fireplace
        staticFields:
          Type:
            defaultValue: '"ISFireplaceInfoAction"'
        fields:
          playerNum:
            type: integer
          fireplace: {}
        methods:
          - name: isValid
            return:
              - type: boolean
          - name: perform
