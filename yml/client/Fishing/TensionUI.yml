version: "1.1"
languages:
  lua:
    classes:
      Fishing:
        staticFields:
          ISTensionUI:
            type: Fishing.ISTensionUI
      Fishing.ISTensionUI:
        extends: ISUIElement
        local: true
        constructors:
          - parameters:
              - name: player
        staticFields:
          Type:
            defaultValue: '"ISTensionUI"'
        fields:
          val:
            type: number
          playerIndex: {}
          player: {}
          tex: {}
          bckgTex: {}
        methods:
          - name: initialise
          - name: prerender
          - name: render
          - name: updateValue
            parameters:
              - name: val
