version: "1.1"
languages:
  lua:
    tables:
      ISMenuContextBuild:
        staticMethods:
          - name: new
            return:
              - type: ISMenuContextBuild
    classes:
      ISMenuContextBuild:
        extends: ISMenuElement
        local: true
        staticMethods:
          - name: init
          - name: createMenu
